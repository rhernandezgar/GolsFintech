# SECURITY_CHECKLIST.md — Controles preventivos de seguridad

Este archivo **no** registra vulnerabilidades encontradas en este repositorio. Recoge los
controles de seguridad que el diseño de las Fases 1 a 3 exige implementar, cada uno con la
tarea que lo implementa y lo que comprueba que quedó implementado.

**Origen de VUL-01 a VUL-08.** Los ocho identificadores provienen del reporte SAST
redactado en la Fase 3 (§3.4), donde se presentan como escenario ilustrativo del tipo de
defectos que este stack suele producir. **No salieron de analizar este código: nadie los
encontró aquí.** Son controles preventivos válidos y por eso se conservan; llamarlos
"vulnerabilidades encontradas" sería inexacto, y por eso no están en `BUGS.md`.

**Los identificadores se conservan tal cual** porque están citados en los documentos
entregados de las tres fases y la trazabilidad con ellos debe mantenerse.

Estado: Pendiente / En progreso / Implementado.

## Reglas

1. **Ninguna entrada se borra.** Un control implementado se marca como Implementado en su
   sitio, con la tarea y el commit que lo implementó.
2. **Un control no se mueve a `BUGS.md` al implementarse.** Solo pasa a `BUGS.md` si más
   adelante se descubre que quedó mal implementado y eso constituye un hallazgo real y
   reproducible; en ese caso se abre una entrada nueva allí que cita este ID, y el control
   se queda aquí igualmente.
3. Un control se declara Implementado únicamente cuando existe el commit que lo implementa
   **y** la verificación de la última columna lo acredita.

## Controles

| ID | Control | Origen (fase de diseño) | Tarea que lo implementa (T#) | Estado | CWE / OWASP | Verificación |
|---|---|---|---|---|---|---|
| VUL-01 | Validar el tipo real del archivo cargado por inspección de contenido (finfo / magic bytes), renombrarlo con identificador generado y almacenarlo fuera de la raíz web, en lugar de confiar en la extensión declarada por el cliente. | Fase 3 §3.4 | T4 (adaptador de `DocumentRepository`) y T7 (adaptador de carga) | **Implementado en T7** (2026-08-22) — `Infrastructure/Storage/UploadedDocumentStore`: tipo real por `finfo` contra una lista blanca, nombre generado con UUID y extensión derivada del tipo detectado, disco `local` (storage/app/private, fuera de la raíz web), límite de 5 MB y hash SHA-256 del contenido. El nombre que manda el cliente no se usa para construir ninguna ruta | CWE-434 · A04:2021 | **El script sigue sin cubrir el tipo de archivo**; lo acredita la prueba que el propio control pedía: `DocumentUploadTest::an_executable_renamed_as_an_image_is_rejected` sube un ejecutable con cabecera ELF y extensión `.jpg` y exige 422 con cero filas en `identity_documents`. Acompañan `a_file_over_the_size_limit_is_rejected` y `an_unknown_document_type_is_rejected_on_the_server`. |
| VUL-02 | El catálogo de plazos autorizados vive en el dominio y se valida en el servidor, venga el parámetro de donde venga. | Fase 3 §3.4 | T3 (dominio) y T10 (endpoint) | En progreso — `Domain/Credit/Term::AUTHORIZED_MONTHS` rechaza todo valor fuera del catálogo desde T3 (commit `9e9a8d7`); falta el endpoint que lo consuma | CWE-20 · A03:2021 | `verificar_avance.sh` §T6 «php artisan test --testsuite=Unit en verde», que cubre `TermTest` (7 valores no autorizados). El cierre exige §T10 «N FormRequest(s): validación replicada en el servidor». |
| VUL-03 | Token de acceso en cookie con `HttpOnly`, `Secure` y `SameSite`; vigencia corta y rotación del refresh token. Nunca en `localStorage`, donde queda accesible ante un XSS. | Fase 3 §3.4 | T5 (servidor) y T10 (SPA) | En progreso — el lado del servidor está hecho en T5: `access_token` de 15 minutos y `refresh_token` de 14 días (`config/security.php`, `Passport::tokensExpireIn`), y `DELETE /api/v1/auth/session` revoca **ambos**, no solo el de acceso. **Falta el lado del cliente**: la SPA todavía no tiene código de autenticación, así que la decisión cookie `HttpOnly` frente a `localStorage` no está tomada en código. La rotación del refresh token en cada uso tampoco: hoy Passport revoca el anterior al canjearlo, pero no hay prueba que lo exija | CWE-522 · A07:2021 | `verificar_avance.sh` §T5 «Hay prueba que exige 401 sin token» y «Hay implementacion de PKCE (code_challenge)». **El script no comprueba hoy el almacenamiento del token.** Acreditado en parte por `OAuthPkceFlowTest::the_full_authorization_code_flow_with_pkce_issues_a_usable_token` y `ApiAccessControlTest::closing_the_session_revokes_the_token`; el cierre exige además `grep -rn "localStorage" frontend/src` sin resultados sobre el token. |
| VUL-04 | La bitácora de auditoría nunca conserva CURP, RFC ni PAN en claro: el enmascarado se aplica en el constructor del evento, de modo que no exista forma de construir uno que conserve el dato. | Fase 3 §3.4 | T3 (enmascarado) y T8 (bitácora completa) | **Implementado en T8** (2026-08-22) — `Domain/Audit/SensitiveDataMasker` se aplica en el constructor de `AuditEvent` desde T3 (commit `9e9a8d7`). En T8 se fija además el **orden**: el hash se calcula sobre `$event->metadata`, es decir sobre los metadatos ya enmascarados, de modo que se firma exactamente lo que se almacena. Enmascarar después de firmar dejaría la cadena rota sobre datos legítimos y la bitácora dejaría de servir como evidencia | CWE-532 · A09:2021 | `verificar_avance.sh` higiene transversal #3 «Ningun registro de log incluye CURP, RFC ni numero de tarjeta» y §T8 «Hay prueba que altera la bitacora y exige que se detecte». Acreditado además por `VerifyAuditChainCommandTest::the_masking_happens_before_the_hash_so_a_legitimate_record_verifies` (un evento con CURP se guarda redactado **y** `audit:verify-chain` sigue saliendo en 0) y `::the_hash_is_computed_over_the_masked_metadata_and_not_the_raw_one` (firmar el metadato en claro daría otro hash). |
| VUL-05 | Respuestas de error genéricas con identificador de correlación; el detalle técnico solo en los registros del servidor, nunca en la respuesta al cliente. | Fase 3 §3.4 | T10 | En progreso — desde T3 toda excepcion de dominio separa `getMessage()` (detalle tecnico, solo al registro) de `userMessage()` (mensaje generico al usuario) y expone `errorCode()`; en T4 se sumo `ExternalServiceUnavailableException`, que no nombra al proveedor que fallo. Falta normalizar la respuesta HTTP y anadir el identificador de correlacion | CWE-209 · A05:2021 | `verificar_avance.sh` §T10 «Errores normalizados a mensaje generico con identificador de correlacion» y «Los controladores no devuelven getMessage()/traza al cliente». |
| VUL-06 | Auditoría de dependencias incorporada a la rutina de entrega: `composer audit` y `npm audit` se ejecutan y sus hallazgos se registran y se corrigen actualizando versiones. | Fase 3 §3.4 | T12 | Pendiente — los comandos ya se ejecutan desde la auditoría y hoy no reportan nada (ver `BUGS.md`, análisis ejecutados); falta fijarlos como rutina | CWE-1395 | `verificar_avance.sh` §T12 «composer audit: sin vulnerabilidades declaradas» y «npm audit (frontend/ y worker/): sin vulnerabilidades». |
| VUL-07 | Límite de intentos por identificador y por dirección IP en la consulta de estatus de solicitud. | Fase 3 §3.4 | T5 (acceso) y T11 (consulta de estatus) | En progreso — el control existe ya sobre el endpoint de acceso: `LoginController` limita a 5 intentos con clave derivada de **correo + IP a la vez**, de modo que ni el ataque a una cuenta desde muchas direcciones ni el barrido de muchas cuentas desde una sola pasan por debajo del umbral. **Falta el endpoint de consulta de estatus de solicitud**, que es el que nombra el control y que no existe hasta T10 | CWE-307 · A04:2021 | `verificar_avance.sh` §T11 «Rate limiting definido (RateLimiter::for / throttle:)». Acreditado hoy por `TwoFactorLoginTest::repeated_attempts_against_one_account_are_throttled`, que exige 429 en el sexto intento. |
| VUL-08 | Cabeceras `Strict-Transport-Security`, `Content-Security-Policy`, `X-Content-Type-Options` y `Referrer-Policy` en todas las respuestas, por middleware de la aplicación. | Fase 3 §3.4 | T11 | Pendiente | CWE-1021 · A05:2021 | `verificar_avance.sh` §T11 «El middleware define las 4 cabeceras», «Middleware registrado en backend/bootstrap/app.php» y «curl -I devuelve las 4 cabeceras». |

## Controles del diseño sin identificador `VUL-xx`

La serie `VUL-xx` corresponde al reporte SAST de la Fase 3 §3.4 y **no puede ampliarse
aquí**: `VUL-09` en adelante son hallazgos verificados y viven en `BUGS.md` (CLAUDE.md
§6.2). Los controles que el diseño exige y que ese reporte no numeró se registran con el
identificador del requerimiento que los origina, que es un identificador real de las
Fases 1 y 2, no uno inventado para este archivo.

| ID | Control | Origen (fase de diseño) | Tarea que lo implementa (T#) | Estado | CWE / OWASP | Verificación |
|---|---|---|---|---|---|---|
| RS-01.a | OAuth 2.0 con flujo de código de autorización y PKCE **S256** para el cliente público. El flujo implícito, descartado. | Fase 3 §4.4 | T5 | Implementado — `laravel/passport` ^13.7; `EnforcePkceS256` exige `code_challenge` y rechaza `plain`, la omisión del método (que RFC 7636 §4.3 resuelve como `plain`), `response_type=token` y `grant_type=implicit`. El cliente de la SPA se crea con `confidential: false`, sin secreto | CWE-319 · A02:2021 | `verificar_avance.sh` §T5 «Hay implementacion de PKCE (code_challenge)». `OAuthPkceFlowTest`: 12 pruebas, incluidas `the_plain_code_challenge_method_is_rejected`, `omitting_the_method_does_not_fall_back_to_plain`, `an_intercepted_code_is_useless_without_the_verifier` y `an_implicit_authorization_request_is_rejected`. |
| RS-01.b | Segundo factor TOTP obligatorio para todo perfil administrativo. | Fase 3 §4.4 | T5 | Implementado — `Infrastructure/Security/TotpAuthenticator` (RFC 6238). Un perfil administrativo sin segundo factor dado de alta **queda fuera**, no exento. Secreto cifrado a nivel de columna | CWE-308 · A07:2021 | `verificar_avance.sh` §T5 «Hay implementacion de 2FA». `TotpAuthenticatorTest` reproduce los vectores del apéndice B de RFC 6238; `TwoFactorLoginTest::an_administrative_profile_without_a_second_factor_is_locked_out`. |
| RS-01.c | Contraseñas con Argon2id y sal única por credencial. | Fase 3 §4.5 | T5 | Implementado — `config/hashing.php` con driver `argon2id`, 64 MiB y 4 pasadas (recomendación de OWASP). Los parámetros viajan dentro del hash: subirlos no invalida las credenciales existentes | CWE-916 · A02:2021 | **El script no cubre hoy el algoritmo de las contraseñas.** Acreditado por `TwoFactorLoginTest::passwords_are_stored_with_argon2id`, que exige el prefijo `$argon2id$` en la columna. |
| RS-05.a | RBAC de cinco roles con **permisos por rol**, no solo el nombre: auditor de solo lectura, ingresos declarados restringidos al analista de riesgos. | Fase 3 §4.9 (tabla de alcance) | T5 | Implementado — `Domain/Access/Role` y `Domain/Access/Permission` trasladan la tabla de la Fase 3 al dominio. `Role::isReadOnly()` se **deriva** de los permisos, de modo que no puede contradecirlos. No hay ningún `Gate::before` que conceda todo a un rol | CWE-1220 · A01:2021 | `verificar_avance.sh` §T5 «Los 5 roles RBAC aparecen en el codigo» y «Hay prueba que exige 403 con rol insuficiente». `RoleAccessMatrixTest` (17 pruebas, sin base de datos) y `ApiAccessControlTest::the_auditor_cannot_write_even_over_a_record_it_may_read`. |
| RS-05.b | **Autorización a nivel de objeto**: no basta con poder consultar solicitudes, hay que tener derecho sobre la solicitud concreta. | Fase 3 §4.9 (párrafo explícito) | T5 | Implementado — `Policies/CreditApplicationPolicy` decide sobre el registro, no sobre el rol. El usuario se ancla a su prospecto por `users.prospect_id`; sin ese anclaje ninguna comparación resuelve a cierto | CWE-639 (IDOR) · A01:2021 | **El script no distingue hoy el 403 por objeto del 403 por rol**: su §T5 solo exige «403 con rol insuficiente». Acreditado por `ApiAccessControlTest::a_prospect_cannot_read_the_application_of_another_prospect`, `a_prospect_with_no_file_of_its_own_reads_nobody_elses` y `the_object_level_denial_reveals_nothing_about_the_record`. |
| RS-05.c | Visibilidad **por campo** según el rol: los ingresos declarados no viajan a quien no tiene necesidad justificada de conocerlos. | Fase 2 §P7, Fase 3 §4.9 | T5 | Implementado — `CreditApplicationPolicy::viewDeclaredIncome`. El recurso responde 200 y omite el campo, en vez de negar el recurso entero: negarlo obligaría a duplicar endpoints por rol, y ocultarlo solo en la interfaz no sería un control | CWE-200 · A01:2021 | **El script no cubre hoy la autorización por campo.** Acreditado por `ApiAccessControlTest::the_declared_income_of_a_third_party_does_not_travel_to_other_roles` (administrador y auditor) y `only_the_risk_analyst_sees_the_declared_income_of_a_third_party`. |
| RS-06.a | El acceso a información personal es en sí mismo un evento auditable: cada consulta se registra con usuario, fecha e IP. | Fase 2 §P7 | T5 (acceso y consultas) y T8 (bitácora completa) | **Implementado en T8** (2026-08-22) — se escriben `auth.login_succeeded`, `auth.login_failed`, `auth.two_factor_failed`, `auth.session_ended`, `credit_application.viewed` y `audit_log.viewed`, cada uno con actor, `event_at` e `ip_address`. `ip_address` entra en el material del hash, así que no se puede retocar sin romper la cadena | CWE-778 · A09:2021 | `ApiAccessControlTest::reading_an_application_is_itself_an_audited_event` y `TwoFactorLoginTest::a_successful_login_is_recorded_in_the_audit_log`; §T8 «Hay prueba que altera la bitacora y exige que se detecte» y `VerifyAuditChainCommandTest::altering_only_the_ip_address_of_a_record_is_detected`. |
| RS-06.b | La bitácora es **verificable**: cualquier alteración, borrado o inserción posterior a la escritura tiene que ser detectable, y detectable de forma automática. | Fase 1 §RS-06, Fase 2 §5 (encadenamiento) | T8 | **Implementado en T8** (2026-08-22) — `Domain/Audit/AuditChainVerifier` comprueba dos invariantes por registro (que su `previous_hash` sea el `current_hash` del que lo precede, y que su hash recalculado coincida con el almacenado) y `php artisan audit:verify-chain` lo ejecuta sobre la tabla entera, **terminando en código distinto de cero** si la cadena está rota e indicando en qué registro. El material del hash está fijado y documentado en `Domain/Audit/AuditChain` | CWE-345 · A08:2021 | `verificar_avance.sh` §T8 «El comando audit:verify-chain esta registrado» y «audit:verify-chain termina en 0 (cadena integra)». Las tres formas de manipulación las acreditan `AuditChainVerifierTest` (11 pruebas sin base de datos) y `VerifyAuditChainCommandTest` (13 pruebas contra MySQL manipulado con SQL directo), incluida la inserción con los hashes bien recalculados por el atacante. `::the_hashed_columns_cover_the_whole_table` compara la lista de campos firmados contra el esquema real. |
| RNF-07.a | Endpoints autenticados por defecto; las excepciones se declaran de forma explícita. | CLAUDE.md §6 regla 9; Fase 3 §1.4 | T5 | Implementado — cuatro excepciones, todas por necesidad: quien todavía no tiene token no puede presentarlo | CWE-306 · A01:2021 | **El script no cubre hoy esta regla.** Acreditado por `ApiAccessControlTest::the_endpoints_are_authenticated_by_default`, que recorre la tabla de rutas y compara contra una **lista cerrada**: una ruta nueva sin autenticar rompe la prueba. |


## Riesgos residuales aceptados

Un control implementado puede dejar un hueco conocido. Registrarlo aquí no lo convierte en
un hallazgo —no lo es: es una consecuencia del diseño, no un defecto reproducible, y por
eso no va en `BUGS.md`— pero **callarlo sí sería un defecto del expediente**. Una auditoría
quiere ver que el hueco se identificó, se midió y se decidió sobre él.

No reciben identificador propio: se nombran por el control del que son residuo.

| Control | Riesgo residual | Mitigación adoptada | Alternativas descartadas y por qué | Aceptado por / fecha | Qué reabriría la decisión |
|---|---|---|---|---|---|
| RS-06.b (encadenamiento de la bitácora) | La cadena acredita la **consistencia interna** de la bitácora, no su **completitud**. Dos manipulaciones quedan fuera de su alcance: **(a)** borrar los últimos registros, porque lo que queda sigue siendo una cadena coherente y nada dentro de la tabla dice cuántos registros debería haber; **(b)** reescribir por completo el tramo final, porque el hash **no lleva llave** y el material es público, así que quien tenga escritura sobre la tabla puede recalcular una cadena entera desde el punto que quiera manipular. Lo que sí detecta es toda **manipulación parcial**: alteración, borrado intermedio e inserción, esta última incluso cuando el atacante calcula bien los hashes del registro que inserta | **Ancla externa**: `audit:verify-chain` imprime el hash de la punta y acepta `--expect-tip`. Ese hash se guarda **fuera de esta base de datos** (integración continua, vault o acta de revisión) y se le pasa al comando; si la bitácora se truncó o se reescribió, la punta no coincide y el comando termina en código 2. Acreditado por `VerifyAuditChainCommandTest::deleting_the_last_record_needs_the_external_anchor_to_be_detected` | **HMAC con llave en vault**: cerraría la reescritura completa, pero **rompe que un auditor externo pueda recalcular la cadena**, que es una propiedad que sí se quiere conservar —hoy `AuditLogController` publica `previous_hash` y `current_hash` justamente para eso—. Un control de integridad que solo el propio sistema puede verificar vale menos como evidencia frente a un tercero. **Almacenamiento WORM** y **publicación periódica de la punta en un tercero**: resuelven el problema pero se apartan de la arquitectura de la Fase 2 e introducen infraestructura fuera del diseño (CLAUDE.md §8) | Usuario, 2026-08-22, tras T8. Documentado en CLAUDE.md §6.5 | Que la bitácora pase a tener valor probatorio frente a un tercero (autoridad, litigio) o que el modelo de amenazas incorpore al administrador de la base de datos como atacante. Ahí el ancla externa deja de bastar, porque depende de que alguien la haya guardado y la compare |


## Numeración

La serie `VUL-xx` es **continua entre este archivo y `BUGS.md`** y ningún número se
reutiliza: VUL-01 a VUL-08 son controles preventivos y viven aquí; VUL-09 y VUL-10 son
hallazgos verificados sobre este código y viven en `BUGS.md`. El archivo en el que está
una entrada indica su naturaleza; el número indica su identidad.

Los controles del diseño que el reporte SAST de la Fase 3 §3.4 no numeró **no reciben un
`VUL-xx` nuevo**: se identifican con el requerimiento que los origina (`RS-01.a`,
`RS-05.b`, …), que es un identificador real de las Fases 1 y 2. Así la serie `VUL-xx`
conserva el significado que tiene en los documentos entregados y ningún lector puede
confundir un control preventivo con un hallazgo.
